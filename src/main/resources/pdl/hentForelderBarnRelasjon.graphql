query($ident: ID!){
    hentPerson(ident: $ident) {
        forelderBarnRelasjon(historikk: false) {
            relatertPersonsIdent,
            relatertPersonsRolle,
            minRolleForPerson,
        }
        adressebeskyttelse(historikk: false) { gradering }
    }
}