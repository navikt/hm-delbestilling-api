query($ident: ID!){
    hentPerson(ident: $ident) {
        bostedsadresse(historikk: false ) {
            vegadresse {
                kommunenummer
            }
        }
        adressebeskyttelse(historikk: false) {
            gradering
        }
    }
}